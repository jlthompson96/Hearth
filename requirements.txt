# Runtime. Stack is decided in CLAUDE.md — do not substitute.
#
# Ranges, not pins: these were written on a machine that could not run pip, so
# nothing here has been resolved against an index. Run `make freeze` on the box
# that actually installs to turn this into exact pins.

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0
sqlalchemy>=2.0,<3.0
alembic>=1.13,<2.0
psycopg[binary]>=3.2,<4.0
httpx>=0.27,<1.0
pyyaml>=6.0,<7.0

# The LLM does not arrive until Phase 4, but the major versions are decided now
# so Phase 4 is a code change rather than a dependency negotiation.
langchain>=1.0,<2.0
langgraph>=1.0,<2.0
langchain-openai>=1.0,<2.0

# langchain-mcp-adapters lands in Phase 12 and must be pinned exactly when it
# does — langchain.mcp is beta and raises LangChainBetaWarning.
